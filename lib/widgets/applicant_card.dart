import "package:flutter/material.dart";

// detailed view of an applicant
class ApplicantCard extends StatelessWidget {
  const ApplicantCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
