import "package:flutter/material.dart";

//  Overview of all judge votes for an applicant

class DiscussionCard extends StatelessWidget {
  const DiscussionCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
